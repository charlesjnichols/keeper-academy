version: '3.8'

services:
  keeper-slides:
    build: .
    ports:
      - "3030:3030"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - HOST=0.0.0.0
    restart: unless-stopped
    container_name: keeper-academy-slidev
